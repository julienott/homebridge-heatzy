{
  "pluginAlias": "Heatzy",
  "pluginType": "platform",
  "singular": true,
  "headerDisplay": "Enter your credentials for the Gizwits API.",
  "footerDisplay": "Ensure your credentials are correct to enable proper functioning of the plugin.",
  "schema": {
    "type": "object",
    "properties": {
      "username": {
        "title": "Username",
        "type": "string",
        "required": true
      },
      "password": {
        "title": "Password",
        "type": "string",
        "required": true,
        "format": "password"
      },
      "modes": {
        "title": "Spawn Each accessory for the following modes",
        "type": "array",
        "items": {
          "type": "string",
          "enum": ["Confort", "Eco", "Eco Plus", "Sleep", "Antifreeze"]
        },
        "uniqueItems": true,
        "minItems": 1
      }
    }
  },
  "form": null,
  "display": null
}
